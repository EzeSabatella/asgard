[project]
name = "superagente-odin"
version = "0.1.0"
description = "Professional/technical surface agent for SuperAgente."
dependencies = [
    "superagente-shared",
    "langchain",
    "pydantic"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
