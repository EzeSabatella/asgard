[project]
name = "superagente-shared"
version = "0.1.0"
description = "Shared utilities and types for the SuperAgente project."
dependencies = [
    "pydantic",
    "python-dotenv"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
